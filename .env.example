# Copie para `.env` (que é ignorado pelo git) e preencha para usar o chat com
# o plugin OpenCode Go no VS Code (as launch configs usam envFile=.env).
#
# OPENCODE_API_KEY=...
# OPENCODE_GO_MODEL=deepseek-v4.1-flash
